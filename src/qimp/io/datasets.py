"""Dataset loaders for batch image processing."""

from __future__ import annotations

# TODO(Fase 3): migrate from legacy/scripts/generate_gp_images.py
#   - GPImageDataset(root, target_size=(16, 16), channels=("R","G")) — iterable of pairs
#   - batch_process_images(loader, fn, ...) -> list
