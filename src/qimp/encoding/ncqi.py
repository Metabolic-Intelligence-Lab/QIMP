"""NCQI encoding: NEQR extension to RGB images."""

from __future__ import annotations

# TODO(Fase 4): docs/tesi.pdf §3.1.2 (color extension)
#   - ncqi_circuit(image: np.ndarray, q: int) -> QuantumCircuit
#   - ncqi_decode(counts: dict, n: int, q: int) -> np.ndarray
